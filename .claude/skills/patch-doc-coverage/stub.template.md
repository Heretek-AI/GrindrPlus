# <Hook display name>

<One-line: what the hook does and which user-visible feature it powers.>

## Target

<!-- Search markers used to locate this hook's symbols in newer Grindr APKs.
     Copy each `// search for '...'` marker from the hook file as a fenced
     code block, with the captured symbol name as a label. Example:

```
// OnlineIndicator.kt
val utils = "ka8" // search for ' <= 600000;'
val isFeatureFlagEnabled = "xh6" // search for 'implements IsFeatureFlagEnabled'
findClass("com.grindrapp.android.utils.ProfileUtilsV2") // search for 'R.string.profile_time_online_minutes_ago'
```
-->

```kotlin
// <HookFile>.kt
```

## Verified against

- <Grindr version> — initial support
- <Latest verified Grindr version>

## Notes

<!-- Optional: anything a future maintainer should know — which feature
     flag gates this hook, why a non-obvious method-body hook was needed,
     behavior on rooted vs unrooted devices, etc. -->
